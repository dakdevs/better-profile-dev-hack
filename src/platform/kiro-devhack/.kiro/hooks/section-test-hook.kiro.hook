{
  "enabled": true,
  "name": "Section Test & Validation",
  "description": "Monitors completion of code sections and automatically creates test scripts (.js or .sh), runs type checking and syntax validation, then provides a summary and cleans up test files",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "src/**/*.{ts",
      "tsx",
      "js",
      "jsx}",
      "*.{ts",
      "tsx",
      "js",
      "jsx}",
      "**/*.{ts",
      "tsx",
      "js",
      "jsx}"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A section of code has been completed. Please:\n\n1. Create appropriate test scripts (.js or .sh files) to validate the completed section\n2. Run automatic type checking using TypeScript compiler\n3. Perform syntax validation on the modified files\n4. Once tests pass, provide a comprehensive summary of what was completed and validated\n5. Clean up the temporary test files after validation is complete\n\nFocus on practical testing that verifies the code works as intended, including:\n- Type safety validation\n- Syntax correctness\n- Basic functionality tests\n- Integration with existing codebase\n\nProvide the summary in a clear, concise format highlighting what was tested and validated."
  }
}